# CandidateConnect-Field-DEV

Separate Candidate Connect Field App DEV deployment.

C4.3 reads user-scoped assignments from:

`app_state/mobile_assignments/<campaign_id>/<username>.json`

and writes staged field results to:

`app_state/mobile_results/<campaign_id>.json`

This app does not update voter records.
